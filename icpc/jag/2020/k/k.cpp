#include <bits/stdc++.h>

using namespace std;

using ll = long long;

int main() {
    int n;
    string s;
    cin >> n;
    getchar();
    while (n--) {
        getline(cin, s);
        cout << s.size() << endl;
    }

    return 0;
}